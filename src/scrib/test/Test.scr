//Raymond@HZHL3 ~/code/scribble-neon/github-rhu1-go/scribble-go-test
//$ ../scribble-java/bin/scribblec.sh -ip src -d src src/scrib/test/Test.scr -goapi Proto1 A 
//$ go install scrib/test


//http://sandbox.kidstrythisathome.com/erdos/


module scrib.test.Test;  // Use scrib prefix to separate from general go packages


type <go> "int" from "..." as int;


global protocol Proto1(role A, role B)
{
	choice at A
	{
		Ok(int) from A to B;
		PPP() from B to A;
	}
	or
	{
		Bye(int) from A to B;
	}
}


global protocol Proto2(role A, role B)
{
	choice at A
	{
		Ok() from A to B;
	}
	or
	{
		Bye() from A to B;
	}
	choice at A
	{
		Ok() from A to B;
	}
	or
	{
		Bye() from A to B;
	}
}
